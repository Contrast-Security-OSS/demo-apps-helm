{
    "$schema": "http://json-schema.org/schema#",
    "type": "object",
    "properties": {
      "environment": {
        "type": "string",
        "title": "Environment",
        "enum": ["Development", "QA", "Production"],
        "default": "Development",
        "description": "Set the environment directly to override the default set by the Contrast TeamServer"
      }
    }
  }
  